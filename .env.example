# Admin login credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_please

# JWT signing secret (generate with: python -c "import secrets;print(secrets.token_hex(32))")
JWT_SECRET=replace_with_a_long_random_hex_string
JWT_EXPIRE_HOURS=168

# DB field-level encryption key — REQUIRED.
# Generate with: python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
# WARNING: do NOT change or lose this once set; it decrypts all saved API keys and tunnel tokens.
ENCRYPTION_KEY=replace_with_a_fernet_key

# Paths
DATA_DIR=./data
TEMPLATE_IPYNB=./data/template.ipynb

# Server
HOST=0.0.0.0
PORT=8000
